<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Admin Tasks</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            background: linear-gradient(to right, #2193b0, #6dd5ed); 
            min-height: 100vh; 
            padding: 20px; 
        }
        .container { 
            max-width: 1200px; 
            margin: 0 auto; 
            background: #fff; 
            padding: 30px; 
            border-radius: 10px; 
            box-shadow: 0px 4px 10px rgba(0,0,0,0.2); 
        }
        h1 { 
            text-align: center; 
            margin-bottom: 30px; 
            color: #333; 
        }
        table { 
            width: 100%; 
            border-collapse: collapse; 
            margin-bottom: 20px; 
        }
        th, td { 
            padding: 12px; 
            border: 1px solid #ddd; 
            text-align: left; 
        }
        th { 
            background: #2193b0; 
            color: #fff; 
        }
        .btn { 
            padding: 8px 16px; 
            background: #2193b0; 
            color: white; 
            border: none; 
            border-radius: 4px; 
            cursor: pointer; 
            text-decoration: none; 
            display: inline-block; 
        }
        .btn:hover { 
            background: #176d7a; 
        }
        .form-group { 
            margin-bottom: 15px; 
        }
        .form-group label { 
            display: block; 
            margin-bottom: 5px; 
        }
        .form-group input, 
        .form-group select, 
        .form-group textarea { 
            width: 100%; 
            padding: 8px; 
            border: 1px solid #ddd; 
            border-radius: 4px; 
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Manage Tasks</h1>
        
        <!-- Add Task Form -->
        <h2>Add New Task</h2>
        <form th:action="@{/admin/tasks/add}" method="post">
            <div class="form-group">
                <label>Title:</label>
                <input type="text" name="title" required>
            </div>
            <div class="form-group">
                <label>Description:</label>
                <textarea name="description" required></textarea>
            </div>
            <div class="form-group">
                <label>Status:</label>
                <select name="status" required>
                    <option value="PENDING">Pending</option>
                    <option value="IN_PROGRESS">In Progress</option>
                    <option value="COMPLETED">Completed</option>
                    <option value="DELAYED">Delayed</option>
                </select>
            </div>
            <div class="form-group">
                <label>Start Date:</label>
                <input type="date" name="startDate" required>
            </div>
            <div class="form-group">
                <label>Due Date:</label>
                <input type="date" name="dueDate" required>
            </div>
            <div class="form-group">
                <label>Assign To:</label>
                <select name="assignedUser" multiple required id="assignedUsersDropdown" style="width: 100%">
                    <option th:each="user : ${users}" 
                            th:value="${user.id}" 
                            th:text="|${user.name} (${user.email})|">
                    </option>
                </select>
            </div>
            <button type="submit" class="btn">Add Task</button>
        </form>
        
        <hr>
        
        <!-- Tasks List -->
        <h2>All Tasks</h2>
        <table>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Status</th>
                <th>Start Date</th>
                <th>Due Date</th>
                <th>Assigned To</th>
                <th>Actions</th>
            </tr>
            <tr th:each="task : ${tasks}">
                <td th:text="${task.title}"></td>
                <td th:text="${task.description}"></td>
                <td th:text="${task.status}"></td>
                <td th:text="${task.startDate}"></td>
                <td th:text="${task.dueDate}"></td>
                <td th:text="${task.assignedUser != null ? task.assignedUser.name : 'Unassigned'}"></td>
                <td>
                    <a th:href="@{/admin/tasks/updateStatus(taskId=${task.id}, status='COMPLETED')}" class="btn">
                        Mark Completed
                    </a>
                </td>
            </tr>
        </table>
        
        <a th:href="@{/admin/dashboard}" class="btn">Back to Dashboard</a>
    </div>
</body>
</html>
