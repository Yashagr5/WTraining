package com.company.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Configuration class for additional beans if needed
    // PasswordEncoder is already defined in SecurityConfig
}
